import 'dart:ui';

class AppColors {
  static const Color primary = Color(0xff236b60);
  static const Color white = Color(0xffffffff);
  static const Color backgroundScaffold = Color(0xfff5f6f8);
  static Color selectionText = Color(0x00000000).withValues(alpha: 0.1);
  static Color whiteOpacity = Color(0xffffffff).withValues(alpha: 0.1);
  static Color shadow = Color(0x00000000).withValues(alpha: 0.5);
}
